namespace ZSnaper.Models;

/// <summary>
/// Controls which visual variant is used for the tray icon.
/// </summary>
public enum TrayIconStyle
{
    FollowTheme,
    Light,
    Dark,
    CustomSvg,
    LegacyBlack
}
